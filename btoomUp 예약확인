/**
 * [기능 2] 예약 현황 조회 서비스
 */
public void showAllAppointments() {
    System.out.println("\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");
    System.out.println("     📅 현재 예약 시스템 현황");
    System.out.println("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");

    // 1. 데이터 존재 여부 확인 (DB가 비어있는지 체크)
    if (dbMock == null || dbMock.isEmpty()) {
        System.out.println("현재 등록된 예약 내역이 존재하지 않습니다.");
        return;
    }

    // 2. 전체 리스트 순회 및 출력 (SELECT * FROM 실행 결과 출력)
    int count = 0;
    for (Appointment app : dbMock) {
        count++;
        System.out.printf("[%d번 예약] 환자번호: %d | 의사번호: %d | 예약시간: %s%n", 
                          app.getId(), app.getpId(), app.getdId(), app.getAppTime());
    }

    System.out.println("------------------------------");
    System.out.println("총 " + count + "건의 예약이 조회되었습니다.");
}
